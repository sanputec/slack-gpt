# Slack GPT Bot

這是一個部署在 Render 上的 Slack Bot，可以透過私訊使用 GPT-3.5。

## 環境變數

- `OPENAI_API_KEY`: 你的 OpenAI 金鑰
- `SLACK_BOT_TOKEN`: Slack Bot 的 OAuth Token

## 部署平台

建議使用 [Render](https://render.com)，免費支援 Flask Web App。
